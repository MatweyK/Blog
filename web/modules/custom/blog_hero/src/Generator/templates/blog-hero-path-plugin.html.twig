<?php

namespace Drupal\{{ machine_name }}\Plugin\BlogHero\Path;

use Drupal\blog_hero\Plugin\BlogHero\Path\BlogHeroPathPluginBase;

/**
 * Hero block for path.
 *
 * @BlogHeroPath(
 *   id = "{{ plugin_id }}",
 *   match_type = "{{ match_type }}",
 *   match_path = {"<front>"}
 * )
 */
class {{ name }} extends BlogHeroPathPluginBase {

{% if is_title %}
  /**
   * {@inheritdoc}
   */
  public function getHeroTitle() {
    return $this->getPageTitle();
  }

{% endif %}
{% if is_subtitle %}
  /**
   * {@inheritdoc}
   */
  public function getHeroSubtitle() {

  }

{% endif %}
{% if is_image %}
  /**
   * {@inheritdoc}
   */
  public function getHeroImage() {

  }

{% endif %}
{% if is_video %}
  /**
   * {@inheritdoc}
   */
  public function getHeroVideo() {

  }

{% endif %}
}
